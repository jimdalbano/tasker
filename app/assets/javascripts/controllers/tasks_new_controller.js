App.TaskNewController = Em.ObjectController.extend({
	// One of the effects of not useing a discrete route to handle
	// a 'new' task is that Ember doesn't know how create one for 
	// us automagically. Since we still need the controller
	// we need this file.
})